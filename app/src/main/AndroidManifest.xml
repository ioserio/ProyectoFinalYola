<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permiso de Internet para consumir la API -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.Final_2025Merino"
        tools:targetApi="31">
        <!-- Pantalla de inicio -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Menú principal -->
        <activity android:name=".DashboardActivity" android:exported="false" />
        <!-- Secciones -->
        <activity android:name=".NewInventoryActivity" android:exported="false" />
        <activity android:name=".InventoriesListActivity" android:exported="false" />
        <activity android:name=".ProductsListActivity" android:exported="false" />
        <!-- Detalle de inventario -->
        <activity android:name=".InventoryDetailActivity" android:exported="false" />
        <activity android:name=".CategoriesActivity" android:exported="false" />
        <activity android:name=".IngresosActivity" android:exported="false" />
        <activity android:name=".WarehousesActivity" android:exported="false" />

        <!-- Conservamos MainActivity si se usa en otro flujo (no launcher) -->
        <activity android:name=".MainActivity" android:exported="false" />
    </application>

</manifest>